# apiVersion: v1
# kind: Service
# metadata:
#   name: {{ include "mixmode.fullname" . }}
#   labels:
#     {{- include "mixmode.labels" . | nindent 4 }}
# spec:
#   type: {{ .Values.service.type }}
#   ports:
#     - port: {{ .Values.service.port }}
#       targetPort: http
#       protocol: TCP
#       name: http
#   selector:
#     {{- include "mixmode.selectorLabels" . | nindent 4 }}
