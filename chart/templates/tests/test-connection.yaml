apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mixmode.fullname" . }}-test-connection"
  labels:
    {{- include "mixmode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mixmode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
