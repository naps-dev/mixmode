apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: mixmode
  namespace: mixmode
spec:
  interval: 1m
  url: https://github.com/naps-dev/mixmode.git
  ref:
    branch: "###ZARF_VAR_BRANCH###"